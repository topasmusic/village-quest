package de.quest.access;

/** Runtime contract injected into forging menus without living inside the reserved Mixin package. */
public interface ForgingQuickMoveState {
    boolean villageQuest$isQuickMovingResult();
}
